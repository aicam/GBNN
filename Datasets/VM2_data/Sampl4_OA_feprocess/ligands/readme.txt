All ligands were drawn in 2D using Vdraw and written out as mol files (o1.mol, o2.mol, ...)

Vconf was run on each of them using the Windows gui to produce the c?_vconf.sdf and o?_vconf.sdf files. These were then combined into the files cb7_ligands.sdf and oa_ligands.sdf (manually).
